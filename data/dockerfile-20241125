RUN (gem install jekyll -v 3.10.0)
RUN (gem install jekyll-sass-converter -v 1.5.2)
RUN (gem install kramdown -v 2.4.0)
RUN (gem install kramdown-parser-gfm -v 1.1.0)
RUN (gem install jekyll-commonmark-ghpages -v 0.5.1)
RUN (gem install liquid -v 4.0.4)
RUN (gem install rouge -v 3.30.0)
RUN (gem install github-pages-health-check -v 1.18.2)
RUN (gem install jekyll-redirect-from -v 0.16.0)
RUN (gem install jekyll-sitemap -v 1.4.0)
RUN (gem install jekyll-feed -v 0.17.0)
RUN (gem install jekyll-gist -v 1.5.0)
RUN (gem install jekyll-paginate -v 1.1.0)
RUN (gem install jekyll-coffeescript -v 1.2.2)
RUN (gem install jekyll-seo-tag -v 2.8.0)
RUN (gem install jekyll-avatar -v 0.8.0)
RUN (gem install jekyll-remote-theme -v 0.4.3)
RUN (gem install jekyll-include-cache -v 0.2.1)
RUN (gem install jemoji -v 0.13.0)
RUN (gem install jekyll-mentions -v 1.6.0)
RUN (gem install jekyll-relative-links -v 0.6.1)
RUN (gem install jekyll-optional-front-matter -v 0.3.2)
RUN (gem install jekyll-readme-index -v 0.3.0)
RUN (gem install jekyll-default-layout -v 0.1.5)
RUN (gem install jekyll-titles-from-headings -v 0.5.3)
RUN (gem install minima -v 2.5.1)
RUN (gem install jekyll-swiss -v 1.0.0)
RUN (gem install jekyll-theme-primer -v 0.6.0)
RUN (gem install jekyll-theme-architect -v 0.2.0)
RUN (gem install jekyll-theme-cayman -v 0.2.0)
RUN (gem install jekyll-theme-dinky -v 0.2.0)
RUN (gem install jekyll-theme-hacker -v 0.2.0)
RUN (gem install jekyll-theme-leap-day -v 0.2.0)
RUN (gem install jekyll-theme-merlot -v 0.2.0)
RUN (gem install jekyll-theme-midnight -v 0.2.0)
RUN (gem install jekyll-theme-minimal -v 0.2.0)
RUN (gem install jekyll-theme-modernist -v 0.2.0)
RUN (gem install jekyll-theme-slate -v 0.2.0)
RUN (gem install jekyll-theme-tactile -v 0.2.0)
RUN (gem install jekyll-theme-time-machine -v 0.2.0)
RUN (gem install github-pages -v 232)
RUN (gem install html-pipeline -v 2.14.3)
RUN (gem install sass -v 3.7.4)
RUN (gem install safe_yaml -v 1.0.5)
RUN (gem install nokogiri -v 1.16.7)
RUN (gem install webrick)
RUN (gem install rexml)
